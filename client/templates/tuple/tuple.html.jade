template(name="tuple")
  .container-fluid.tuple-default.tuple
    .row
      .col-xs-12
        with tuple
          form.panel.panel-default
            .panel-heading
              .form-group
                label Tuple name
                .input-group
                  +input(family="tuple" _id=_id property="name" value=name isNew=isNew)
                  .input-group-btn
                    button.btn.btn-default.dropdown-toggle(data-toggle='dropdown', type='button')
                      span.caret
                    ul.dropdown-menu.pull-right
                      li
                        a.confirm(href="/tuple/{{_id}}/remove", data-confirmation="Are you sure you want to delete this Tuple file?")
                          i.fa.fa-trash-o
                          | Delete this Tuple File
            .panel-body
              .form-group
                label Note
                +textarea(family="tuple" _id=_id property="note" value=note isNew=isNew)
              .form-group
                label IP addresses
                +textarea(family="tuple" _id=_id property="contents" value=contents isNew=isNew)
              .form-group
                button.btn.btn-primary(type="submit") Save
                span.save-notice.notice.text-success Successfully saved
                span.create-notice.notice.text-success Successfully created
              if queries.count
                hr
                h4 Queries using this Tuple file
                ul
                  each queries
                    li
                      a(href="{{path}}") {{displayName}}
