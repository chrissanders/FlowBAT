template(name="textarea")
  textarea(class="{{family}}-{{property}}-editor {{family}}-property-editor property-editor form-control {{#if isNew}}is-new{{/if}}", name="{{property}}", placeholder="{{placeholder}}", data-family="{{family}}", data-object-id="{{_id}}")
    | #{value}
