template(name="index")
  .container.index
    .row
      .col-xs-12
        h3 Welcome!
        p Please select the meeting from menu above or the following list:
        ul
          each meetings
            li
              a(href=path) #{name}
